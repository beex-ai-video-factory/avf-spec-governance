# Phase Prompt — P1 Single Shot Vertical Slice

Build the smallest end-to-end production-shaped slice:

ShotVersion -> PromptVersion -> GenerationJob -> Provider -> Take -> Persist.

Use FakeProvider first, then approved Google Flow execution track(s).

Required: idempotency, provenance, restart safety and download persistence.

No creative automation required.
